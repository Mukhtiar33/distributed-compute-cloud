# Shared Protocol Definitions

Empty in Phase 0. Document 18 §1 specifies gRPC + mTLS between the Control
Plane and Worker Agent, code-generated from `.proto` files here. These
arrive in Phase 1 (Worker Agent enrollment + heartbeat, Document 17) once
there is an actual client/server contract to define — adding placeholder
`.proto` files now with no consumer would violate Document 17 Rule 1 (no
fake/shortcut scaffolding that gets rebuilt later).
